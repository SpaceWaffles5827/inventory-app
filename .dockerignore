# Ignore DB data / sockets
mysql/
*.sock

# Ignore node_modules (reinstall inside container)
node_modules
.pnpm-store

# Host-only pnpm workspace file (build-script approvals); confuses pnpm in-container
pnpm-workspace.yaml
npm-debug.log
yarn-debug.log
yarn-error.log

# Ignore docker files not needed
Dockerfile*
docker-compose*.yml

# Git / IDE junk
.git
.gitignore
*.md
.vscode
.idea

dev/

minio/